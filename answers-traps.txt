1. 根据 RiSC-V 的 C 函数调用范式，答案是 a0-a7；
2. 由于编译器的内联，对 f 的调用发生在 0x26 处，对 g 的调用发生在 0x14 处；
3. printf 函数位于 0x630；
4. ra 的值为 0x38；
5. 输出为 HE110 World；如果采用大端法，则 i 应设置为 0x726c6400；不需要修改 57616；
6. y=后输出的值没有传递参数，但是 printf 会自行从 a2 寄存器中寻找参数，因此输出寄存器 a2 的值;
